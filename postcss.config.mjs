// const config = {
//   plugins: ["@tailwindcss/postcss"],
// };

// export default config;
const config = {
  plugins: {
    "@tailwindcss/postcss": {}, // this is all you need for Tailwind v4
  },
};

export default config;
